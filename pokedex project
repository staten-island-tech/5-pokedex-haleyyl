import json
pokedex = open("./pokedex.json", encoding="utf8")
data = json.load(pokedex)

''' ask = int(input("what pokemon do you want?"))
value = data[ask]["id"]
lang  = input("what language?")
print(data[ask-1]["name"][lang]) '''

''' pokemon = input("what type of pokemon do you want?")
type = False
for mon in data:
    if pokemon in mon["type"]:
        print(mon["name"])
        type = True
if type == False:
    print("No such pokemon") '''

''' search = input("What pokemon are you looking for?")
type = False
for mon in data:
    if search in mon["name"]["english"]:
        print(mon["name"]["english"])
        type = True
if type == False:
    print("No pokemon was found")
 '''
import json
pokedex = open("./moves.json", encoding="utf8")
data = json.load(pokedex)

pokemon = input("What type of moves do you want to see?")
type = False
for mon in data:
    if pokemon in mon["type"]:
        print(mon["ename"])
        type = True
if type == False:
    print ("There is no type.")
